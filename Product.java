package product;

import java.util.Scanner;

public class Product {

    public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        /*

        //Empty constructor with basic Vars.
        ProductData x = new ProductData() {};
        x.Print();

        //constructor add here by me .
        ProductData x1 = new ProductData(1200, "bata") {};
        System.out.print("Before Setter and Getter ->  ");
        x1.Print();

        //Edit Data in constructor to certian value using Setter and Getter.
        System.out.print("After Setter and Getter ->   ");
        x1.setCost(120);
        x1.setName("banana");

        x1.Print();

        System.out.println("The Total Count is = " + ProductData.count);

        // Constructor used to print data given by User Directly.
        int price = scanner.nextInt();
        String name = scanner.next();

        ProductData x2 = new ProductData(price, name);
        x2.Print();
        
        
        ProductData2 x4 = new ProductData2(120,"batats",20);
        
        x4.print();
        
         
        System.out.println("Enter the type of product (normal, digital):");
        String productType = scanner.nextLine().trim().toLowerCase();

        ProductInterface product;

        switch (productType) {
            case "normal": {
                
                System.out.println("Price of the product:");
                int cost = scanner.nextInt();
                scanner.nextLine();

                System.out.println("Name Of Product:");
                String name = scanner.next();

                System.out.println(" weight of the product:");
                int weight = scanner.nextInt();

                product = new ProductData2(cost, name, weight);
                break;
            }
            case "digital": {
                System.out.println("Enter the cost of the digital product:");
                int cost = scanner.nextInt();
                product = new DigitalProduct(cost);
                break;
            }
            default:
                System.out.println("Invalid product type");
                return;
        }

        product.print();
            */
        
        
        
        
        
    }


}
